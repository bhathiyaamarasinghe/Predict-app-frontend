const DEV_URL = 'http://localhost:5002'
const PROD_URL = 'http://localhost:5002'

const baseURL = DEV_URL;

export const PDF_UPLOAD = baseURL + '/pdf-upload';
export const CONFIG_UPLOAD = baseURL + '/config-upload';